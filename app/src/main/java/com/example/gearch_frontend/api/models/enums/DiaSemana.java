package com.example.gearch_frontend.api.models.enums;

// Enum con los dias de la semana
// Debe coincidir exactamente con el enum DiaSemana del backend
public enum DiaSemana {
    LUNES,
    MARTES,
    MIERCOLES,
    JUEVES,
    VIERNES,
    SABADO,
    DOMINGO
}
